{
  "routes": [
    {
      "method": "GET",
      "path": "/event-photos",
      "handler": "event-photo.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/event-photos/count",
      "handler": "event-photo.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/event-photos/:id",
      "handler": "event-photo.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/event-photos",
      "handler": "event-photo.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/event-photos/:id",
      "handler": "event-photo.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/event-photos/:id",
      "handler": "event-photo.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
